services:
  - type: web
    name: day4-flask-board
    runtime: python
    rootDir: .
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -b 0.0.0.0:$PORT app:app
    envVars:
      - key: DATABASE_PATH
        value: /var/data/board.db
    disk:
      name: board-data
      mountPath: /var/data
      sizeGB: 1
